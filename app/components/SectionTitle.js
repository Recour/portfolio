const SectionTitle = ({ text }) => {
  return (
    <div className='sticky lg:hidden top-0 w-screen -mx-6 p-6 bg-slate-900/75 backdrop-blur	text-slate-200 uppercase tracking-widest text-sm font-bold'>{text}</div>
  )
}

export default SectionTitle;